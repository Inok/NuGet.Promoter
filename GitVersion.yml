tag-prefix: '[vV]'
assembly-versioning-scheme: Major
mode: ContinuousDeployment

branches:
  main:
    regex: ^main$
    label: main
    increment: Patch
    prevent-increment:
      of-merged-branch: true
